name: sportify_amateur
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  http: ^0.13.4         # Comunicación HTTP con el backend en NestJS
  flutter_bloc: ^8.1.2  # Opcional, para manejar el estado de la app
  google_sign_in: ^6.1.0
  flutter_facebook_auth: ^5.0.10
  shared_preferences: ^2.2.3
  font_awesome_flutter: ^10.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
